.Page
  .HeadContent
    = render "devise/shared/header"

  .MainContent
    .MainContent__Signin
      会員情報入力
    = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
      = render "devise/shared/error_messages", resource: resource
      .MainContent__Signin__Form
        = f.label :ニックネーム, class: 'FormTitle'
        = f.label :必須, class: 'Mandatory'
        %br/
        = f.text_field :nickname, placeholder: "例）フリマ太郎", autofocus: true, class: 'FormBar'
      .MainContent__Signin__Form
        = f.label :Eメール, class: 'FormTitle'
        = f.label :必須, class: 'Mandatory'
        %br/
        = f.email_field :email, placeholder: "PC•携帯どちらでも可", autocomplete: "email", class: 'FormBar'
      .MainContent__Signin__Form
        = f.label :パスワード, class: 'FormTitle'
        = f.label :必須, class: 'Mandatory'
        -# - if @minimum_password_length
        -#   %em
        -#     (#{@minimum_password_length} characters minimum)
        %br/
        = f.password_field :password, placeholder: "7文字以上の半角英数字", autocomplete: "new-password", class: 'FormBar'
      .MainContent__Signin__Form
        = f.label :確認用パスワード, class: 'FormTitle'
        = f.label :必須, class: 'Mandatory'
        %br/
        = f.password_field :password_confirmation, placeholder: "確認用", autocomplete: "new-password", class: 'FormBar'
      .Person_Info
        .Person_Info__Head
          本人確認
        安心・安全にご利用いただくために、お客さまの本人情報の登録にご協力ください。
        他のお客さまに公開されることはありません。
      .RealName
        お名前（全角）
        = f.label :必須, class: 'Mandatory'
        .RealName__Form
          .RealName__Form--Field
            = f.text_field :real_family_name, placeholder: "例）山田", class: 'FormBar'
          .RealName__Form--Field
            = f.text_field :real_last_name, placeholder: "例）太郎", class: 'FormBar'
      .RealNameKana
        お名前カナ（全角）
        = f.label :必須, class: 'Mandatory'
        .RealNameKana__Form
          .RealNameKana__Form--Field
            = f.text_field :real_family_name_kana, placeholder: "例）ヤマダ", class: 'FormBar'
          .RealNameKana__Form--Field
            = f.text_field :real_last_name_kana, placeholder: "例）タロウ", class: 'FormBar'
      .MainContent__Signin__Form
        = f.label :生年月日, class: 'FormTitle'
        = f.label :必須, class: 'Mandatory'
        %br/
        = f.date_field :birth, class: 'FormBar'

      .Actions
        = f.submit "次へ", class: 'CommitBtn'

  .FootContent
    = render "devise/shared/footer"
-# メルカリでは、送付先住所や支払い情報はマイページからのリンクになっている。要検討。